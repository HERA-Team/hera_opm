# This is a (bad) sample configuration. The `makeflow_type` is not a
# recognized type (analysis, lstbin, etc.). This file should only be used
# for testing, and not as a template.

[Options]
makeflow_type = "bad_type"
pols = ["xx", "xy", "yx", "yy"]
path_to_do_scripts = "~/hera/hera_op/hera_op/data/sample_task_scripts"
conda_env = "hera"
ex_ants = "~/hera/hera_cal/hera_cal/calibrations/herahex_ex_ants.txt"
base_mem = 10000
base_cpu = 1
